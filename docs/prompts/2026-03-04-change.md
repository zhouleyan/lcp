# 修改点

## APIServer Scope 传入
1. app/lcp-server/handler/handler.go 中，Scope 不要跟 lib/rest 有关联，直接在 APIServerHandler 初始化
2. 在 createHandler 函数中无需传入 scope

## 资源

### 嵌套子资源 API 路由如何注册
如 /workspaces/{workspaceId}/namespaces/{namespace}/users/{userId}

### 非增删改查的特定操作如何处理
如 Host（虚拟机）对象，虚拟机启动、重启、扩缩容等操作 API 如何设计，如何兼容现有代码架构

## lib/rest/apigroup.go
1. ResourceInfo 的 Storage 属性到底是初始化 Storage 还是 Store ？实际传入的是 Store ，且不要定义为 interface{} 类型
2. SubResourceInfo 类型已经没有必要，直接定义 SubResources []即可ResourceInfo
3. NewAPIGroupInfo 无需传入各类 Store ，实际上在 NewAPIGroupInfo 函数内直接初始化以来的 Store 即可
4. main.go 里调用 NewAPIGroupInfo 让 main.go 代码太繁杂，所有模块（APIGroup）构建都放到 pkg/apis/install.go 集中处理

## 注意事项
1. 所有改动方案需要跟我确定，并告诉我推荐哪种模式